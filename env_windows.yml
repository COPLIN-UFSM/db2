name: db2
channels:
  - defaults
  - conda-forge
dependencies:
  - pip
  - python=3.12.*
  - twine=4.*
  - setuptools
  - numpy
  - pandas
  - pytest
  - sphinx
  - pip:
      - ibm_db
      - sphinx-press-theme
      - coverage
      - build